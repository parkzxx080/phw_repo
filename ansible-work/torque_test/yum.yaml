- name : lib packages
  hosts : phw1
  tasks :
  - name : install libtool 
    yum :
     name : libtool
     state : installed
  - name : install openssl-devel         
    yum :
     name : openssl-devel
     state : installed 
  - name : install libxml2-devel         
    yum :
     name : libxml2-devel
     state : installed
  - name : install boost-devel         
    yum :
     name : boost-devel
     state : installed
  - name : install gcc         
    yum :
     name : gcc
     state : installed
  - name : install gcc-c++         
    yum :
     name : gcc-c++
     state : installed
